   <!-- BLOG -->
        <!-- section#blog.container.my-4.mb-4>h2.text-center.mb-3.mt-1{Son 6 Blog}+div.row>(div.col-md-4.mb-4>div.card.blog-card>img.card-img-top+div.card-body>h5.card-title.text-center.h1.text-uppercase{başlık-$}+p.card-text.line-clamp8{içerik-$}+a.btn.btn-primary{Daha Fazla})*9 -->
        <section id="blog" class="container container-fluid44 my-4 mb-4">
          <h2 class="text-center text-uppercase mt-3 mb-5 text-light display-5">
            <a class="dropdown-item" href="/blog">Bloglar</a>
          </h2>
          
          <h4 class="text-center mb-3 mt-1" style="color: white"><i class="fa-solid fa-blog me-2"></i>Son 6 Blog</h4>
          <!-- row -->
          <div class="row">
                <!-- server.ts gelen veriler -->
            <% if( typeof blogPosts !== 'undefined' && Array.isArray(blogPosts) && blogPosts.length >0) { %>
              <!-- son 6 blog gelsin -->
            <% blogPosts.slice(-6).forEach(function(post){%>
                <!-- card -->
                <div class="col-md-4 mb-4" id="card_body">
                  <div class="card blog-card h-100">
                    <img src="<%= post.image %>" alt="<%= post.image %>" class="card-img-top img-fluid" />
                    <div class="card-body d-flex flex-column">
                      <h5 class="card-title text-center h1 text-uppercase"> <%= post.title %> </h5>
                      <p class="card-text line-clamp8">   <%= post.content ? post.content.substring(0,400) : '' %>... </p>
                      <div class="text-center mt-auto mb-4">
                         <a href="/blog/<%= post.id%>" class="btn btn-primary">📖  Daha Fazla</a>
                      </div>
                     <div class="card-footer mt-3-text-muted d-flex flex-wrap justify-content-between align-items-center">
                        <span class="d-flex align-items-center"><i class="fas fa-user me-1"></i> <%= post.author %></span>
                        <span class="d-flex align-items-center"><i class="fas fa-folder me-1"></i> <%= post.category %></span>
                        <div class="tag-cloud d-flex flex-wrap">
                          <% post.tags.split(",").forEach(function(tag){%>
                        <span class="badge bg-primary me-1 mb-1"><i class="fas fa-tag"></i> <%= tag.trim() %></span>
                            <%})%>
                            </div>
                            <span class="d-flex align-items-center"><i class="fas fa-calendar-alt me-1"></i> <%= new Date(post.createdAt).toLocaleDateString('tr-TR') %></span>
                      </div>
                    </div> <!-- card-body -->
                  </div> <!-- card -->
                </div> <!-- col-md-4 -->
            <% }) %>
            <% } else {%>
              <h3 class="text-center text-danger">Henüz blog yazısı eklenmemiş</h3>
            <% } %>
          </div>
        </section>